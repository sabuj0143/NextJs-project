import React from 'react';

const Template = ({children}) => {
    return (
        <div>
            {/* <button>WOW</button> */}
            {children}
        </div>
    );
};

export default Template;